export default function Logo({id}) {
  return(
    <a href="#content-home">
    <img id={id} src="/Logo-portfolio.png" style={{width: "300px", cursor:"pointer"}} alt="Logo do portfolio" />
    </a>

  )
}